name: Build Obsidian Plugin

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true
          target: wasm32-unknown-unknown

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "graph-analysis-wasm -> target"

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Build WASM module
        run: |
          cd graph-analysis-wasm
          wasm-pack build --target web
          cd ..

      - name: Install dependencies
        run: npm ci

      - name: Build plugin
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: plugin-files
          path: |
            dist/main.js
            dist/manifest.json
            dist/assets/styles.css
            dist/graph_analysis_wasm_bg.wasm

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Download build artifacts
        uses: actions/download-artifact@v3
        with:
          name: plugin-files
          path: ./dist
      
      - name: Create release zip
        run: |
          mkdir obsidian-graph-analysis
          cp dist/main.js dist/manifest.json dist/assets/styles.css dist/graph_analysis_wasm_bg.wasm obsidian-graph-analysis/
          zip -r obsidian-graph-analysis.zip obsidian-graph-analysis/
      
      - name: Create GitHub release
        uses: softprops/action-gh-release@v1
        with:
          files: obsidian-graph-analysis.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 